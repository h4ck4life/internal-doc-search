[pytest]
testpaths = tests
markers =
    slow: tests that are slow (deselect with '-m "not slow"')
    qdrant: tests that require a running Qdrant instance (deselect with '-m "not qdrant"')
addopts = -v --tb=short
